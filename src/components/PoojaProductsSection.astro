---
import ProductCard from "./ProductCard.astro";
import { productCatalog } from "../data/productCatalog";
import { loadAuthoritativePrices, withAuthoritativePrice } from "../server/services/ProductPricing";

const priceMap = await loadAuthoritativePrices();

const products = productCatalog
  .filter((product) => product.category === "Pooja Flowers")
  .map((product) => withAuthoritativePrice(product, priceMap));
---

<section id="pooja" class="bg-white px-5 py-14 md:px-8 md:py-16">
  <div class="mx-auto max-w-7xl">
    <div class="mb-9 text-center">
      <p class="fp-eyebrow">
        Fresh Petals Hyderabad
      </p>

      <h2 class="fp-h2 mt-3">
        Pooja Flowers
      </h2>

      <p class="mx-auto mt-4 max-w-2xl text-sm leading-7 text-[#66565D]">
        Fresh pooja flowers, jasmine, flower boxes and temple bulk flowers prepared based on daily market availability.
      </p>
    </div>

    <div class="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">
      {
        products.map((product) => (
          <ProductCard
            name={product.name}
            image={product.image}
            priceLabel={product.priceLabel}
            priceType={product.priceType}
            category={product.category}
            description={product.description}
            slug={product.slug}
            isAvailable={product.isAvailable}
          />
        ))
      }
    </div>

    <div class="mt-12 flex justify-center">
      <a
        href="/categories/pooja-flowers"
        class="fp-btn-block"
      >
        View All Pooja Flowers
      </a>
    </div>
  </div>
</section>